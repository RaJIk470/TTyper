/**
 * main class, the class is responsible for setting visible to the main frame
 */
public class Main {
    /**
     * main method just shows the main frame
     * @param args no args.
     */
    public static void main(String[] args){
        Frame.getInstance().setVisible(true);
    }
}
